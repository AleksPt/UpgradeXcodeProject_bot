# История изменений

## Версия 2.4.3 - Правильная замена иконки в Xcode

### Исправления

**Корректная замена иконки в AppIcon.appiconset**
- Исправлена проблема когда новая иконка добавлялась как "unassigned" в Xcode
- Теперь бот правильно обновляет Contents.json в AppIcon.appiconset
- Иконка заменяется для всех вариантов: Any Appearance, Dark и Tinted
- Автоматически создаются файлы с правильными именами для каждого appearance

### Технические изменения

#### Обновлённые функции:
- `replace_app_icon()` - полностью переписана для правильной работы с Contents.json
  - Читает Contents.json из AppIcon.appiconset
  - Находит все записи с размером 1024x1024
  - Создает файлы с правильными именами для каждого appearance
  - Обновляет поле filename в JSON для каждой записи
  - Сохраняет обновленный Contents.json

### Как это работает:

**Старое поведение:**
- Просто сохранял файл AppIcon-1024.png
- Xcode не знал о новой иконке → "unassigned"

**Новое поведение:**
1. Читает Contents.json
2. Находит все записи 1024x1024:
   - Any Appearance → `AppIcon-1024.png`
   - Dark Appearance → `AppIcon-dark-1024.png`
   - Tinted Appearance → `AppIcon-tinted-1024.png`
3. Создает все необходимые файлы
4. Обновляет filename в JSON
5. Сохраняет Contents.json

**Результат:**
- ✅ Any Appearance - заменена
- ✅ Dark - заменена
- ✅ Tinted - заменена
- ❌ unassigned - больше не появляется

---

## Версия 2.4.2 - Поддержка PNG и улучшенная обработка изображений

### Новые возможности

**Поддержка PNG формата**
- Теперь бот принимает иконки в формате PNG
- PNG автоматически конвертируется в JPG с качеством 95%
- Прозрачный фон PNG заменяется белым цветом
- Поддержка различных режимов PNG (RGBA, LA, P)

**Улучшенные сообщения об ошибках**
- При неверном формате показывается текущий формат изображения
- Добавлено логирование информации о загруженных изображениях
- Более подробные сообщения об ошибках

### Технические изменения

#### Обновлённые функции:
- `handle_photo_or_document()` - добавлена поддержка PNG с конвертацией в JPEG
- Автоматическая конвертация PNG с прозрачностью (альфа-канал заменяется белым фоном)
- Улучшенная обработка ошибок с подробным логированием

#### Обновлённые сообщения:
- `MSG_WAITING_ICON` - указано, что поддерживается PNG
- `MSG_ICON_INVALID_FORMAT` - показывает текущий формат изображения

### Поддерживаемые форматы

**Принимаются:**
- ✅ JPEG/JPG (используется как есть)
- ✅ PNG (конвертируется в JPEG)

**Не принимаются:**
- ❌ WebP (показывается ошибка с текущим форматом)
- ❌ BMP (показывается ошибка с текущим форматом)
- ❌ GIF (показывается ошибка с текущим форматом)
- ❌ Другие форматы

**Примечание:** Когда изображение отправляется как фото (📷), Telegram автоматически конвертирует его в JPEG, поэтому любой формат будет принят. При отправке как файл (📎) сохраняется оригинальный формат и проверяется ограничение.

---

## Версия 2.4.1 - Исправление обработки документов с иконкой

### Исправления

**Обработка документов с изображениями**
- Исправлена проблема, когда отправка иконки как документа вызывала ошибку "Пожалуйста, отправь zip архив"
- Теперь обработчик `handle_document` проверяет состояние `waiting_icon` и передает управление обработчику изображений
- Документы с изображениями корректно обрабатываются в режиме ожидания иконки

### Технические изменения

#### Обновлённые функции:
- `handle_document()` - добавлена проверка состояния ожидания иконки перед проверкой архива

---

## Версия 2.4 - Изменение иконки приложения

### Новые возможности

**Кнопка "🎨 Изменить иконку"**
- Добавлена возможность заменить иконку приложения
- Поддерживаются фотографии и документы с изображениями
- Строгая валидация формата и размера:
  - Формат: JPG/JPEG
  - Размер: 1024x1024 пикселей
- При несоответствии требованиям выводится понятное сообщение с текущими параметрами
- Иконка заменяется в `Assets.xcassets/AppIcon.appiconset`
- Автоматическая конвертация в PNG при замене

### Требования к иконке

**Обязательно:**
- Формат: JPG/JPEG
- Размер: ровно 1024x1024 пикселей

**Сообщения об ошибках:**
- Неверный формат → показывается требование
- Неверный размер → показывается текущий размер и требуемый

### Технические изменения

#### Новые функции:
- `replace_app_icon()` - заменяет иконку в Assets.xcassets/AppIcon.appiconset
- `handle_photo_or_document()` - обработчик фото и документов с изображениями
- `change_icon_callback()` - обработчик кнопки "Изменить иконку"

#### Новые зависимости:
- `Pillow==10.1.0` - для работы с изображениями (проверка формата и размера, конвертация)

#### Обновлённые функции:
- `get_pending_actions_summary()` - добавлено отображение действия смены иконки
- `process_archive_with_actions()` - добавлена поддержка замены иконки
- `show_actions_menu()` - добавлена кнопка "Изменить иконку"
- `get_archive_callback()` - передача пути к иконке, очистка временных файлов
- `reset_callback()` - удаление временного файла иконки
- `back_callback()` - очистка состояния ожидания иконки

#### Новые кнопки:
- `BUTTON_CHANGE_ICON` - "🎨 Изменить иконку"

#### Новые сообщения:
- `MSG_WAITING_ICON` - запрос на загрузку иконки
- `MSG_ICON_INVALID_FORMAT` - ошибка формата
- `MSG_ICON_INVALID_SIZE` - ошибка размера
- `MSG_ICON_WILL_CHANGE` - в списке запланированных действий

#### Обновлённые файлы:
- `requirements.txt` - добавлена зависимость Pillow

---

## Версия 2.3 - Полная информация в финальном сообщении

### Новые возможности

**Расширенное финальное сообщение**
- После обработки архива теперь показывается полная информация:
  - Версия
  - Билд
  - Название приложения
  - Bundle ID
- Независимо от того, какие действия были выбраны, показываются все параметры
- Удобно сразу видеть весь результат работы

### Технические изменения

#### Изменённые функции:
- `process_archive_with_actions()` - теперь возвращает все параметры (версия, билд, название, Bundle ID)
- `get_archive_callback()` - формирует расширенное сообщение со всей информацией

**Было:**
```
✅ Архив обновлен!

Версия: 2.0
Билд: 2
```

**Стало:**
```
✅ Архив обновлен!

Версия: 2.0
Билд: 2
Название: My App
Bundle ID: com.example.app
```

---

## Версия 2.2 - Кнопка "Информация о проекте"

### Новые возможности

**Кнопка "ℹ️ Информация о проекте"**
- Добавлена новая кнопка в меню действий
- При нажатии показывает актуальную информацию о проекте:
  - Версия (MARKETING_VERSION)
  - Билд (CURRENT_PROJECT_VERSION)
  - Название приложения (INFOPLIST_KEY_CFBundleDisplayName)
  - Bundle ID (PRODUCT_BUNDLE_IDENTIFIER)
- Можно в любой момент посмотреть текущую информацию о загруженном архиве
- Кнопка "Назад" для возврата в главное меню

### Технические изменения

#### Новые функции:
- `project_info_callback()` - обработчик кнопки "Информация о проекте"

#### Новые кнопки:
- `BUTTON_PROJECT_INFO` - кнопка для отображения информации

#### Изменённые обработчики:
- `show_actions_menu()` - добавлена кнопка "Информация о проекте"
- `handle_document()` - добавлена кнопка "Информация о проекте"
- `main()` - зарегистрирован обработчик `project_info_callback`

---

## Версия 2.1 - Расширенная информация об архиве

### Новые возможности

**Расширенное отображение информации**
- При загрузке архива теперь показывается полная информация:
  - Версия (MARKETING_VERSION)
  - Билд (CURRENT_PROJECT_VERSION)
  - Название приложения (INFOPLIST_KEY_CFBundleDisplayName)
  - Bundle ID (PRODUCT_BUNDLE_IDENTIFIER)

### Технические изменения

#### Новые функции:
- `read_project_info()` - читает всю информацию из project.pbxproj (версия, билд, название, Bundle ID)

#### Изменённые обработчики:
- `handle_document()` - теперь показывает расширенную информацию об архиве

---

## Версия 2.0 - Режим накопления действий

### Основные изменения

Бот теперь работает в новом режиме: вместо немедленного применения каждого изменения, он накапливает действия и применяет их все сразу при нажатии кнопки "Получить обновлённый архив".

### Новые возможности

1. **Множественные действия за один раз**
   - Можно выбрать несколько действий подряд
   - Все изменения применяются к исходному архиву
   - Не нужно перезагружать архив после каждого действия

2. **Новые кнопки**
   - 📥 "Получить обновлённый архив" - применяет все запланированные действия
   - 🔄 "Начать заново" - сбрасывает все выбранные действия
   - Кнопки появляются только после выбора хотя бы одного действия

3. **Визуальная обратная связь**
   - Список всех запланированных действий отображается в меню
   - Пользователь видит, что именно будет изменено
   - Понятные сообщения с эмодзи для каждого типа действия

### Изменения в работе бота

#### Было (версия 1.0):
1. Загрузить архив
2. Нажать "Увеличить версию" → получить архив
3. Снова загрузить архив
4. Нажать "Изменить название" → получить архив
5. Снова загрузить архив
6. Нажать "Сменить Bundle ID" → получить архив

#### Стало (версия 2.0):
1. Загрузить архив
2. Нажать "Увеличить версию"
3. Нажать "Изменить название" → ввести название
4. Нажать "Сменить Bundle ID" → ввести Bundle ID
5. Нажать "Получить обновлённый архив" → получить архив со ВСЕМИ изменениями

### Технические изменения

#### Новые функции:
- `get_pending_actions_summary()` - формирует список запланированных действий
- `process_archive_with_actions()` - применяет все действия к архиву одновременно
- `show_actions_menu()` - показывает меню с действиями и их статусом
- `increment_version_callback()` - обработчик для добавления действия увеличения версии
- `get_archive_callback()` - обработчик для получения обновлённого архива
- `reset_callback()` - обработчик для сброса всех действий

#### Удалённые функции:
- `process_archive_change_name()` - заменена на `process_archive_with_actions()`
- `process_archive_change_bundle_id()` - заменена на `process_archive_with_actions()`
- `button_callback()` - заменена на `increment_version_callback()` и `get_archive_callback()`

#### Изменённые функции:
- `handle_name_input()` - теперь только добавляет действие, не обрабатывает архив
- `handle_bundle_id_input()` - теперь только добавляет действие, не обрабатывает архив
- `back_callback()` - упрощена, теперь просто возвращает в меню

#### Новые сообщения:
- `MSG_ACTION_ADDED` - уведомление о добавлении действия
- `MSG_VERSION_WILL_INCREMENT` - описание действия увеличения версии
- `MSG_NAME_WILL_CHANGE` - описание действия изменения названия
- `MSG_BUNDLE_ID_WILL_CHANGE` - описание действия изменения Bundle ID

#### Новые кнопки:
- `BUTTON_INCREMENT_VERSION` - заменила `BUTTON_PROCESS_ARCHIVE`
- `BUTTON_GET_ARCHIVE` - новая кнопка для получения архива
- `BUTTON_RESET` - новая кнопка для сброса действий

### Хранение состояния

Для каждого пользователя в `context.user_data` теперь хранятся:
- `archive_{user_id}` - путь к исходному архиву
- `file_name_{user_id}` - имя файла архива
- `action_increment_version_{user_id}` - флаг увеличения версии
- `action_new_name_{user_id}` - новое название приложения
- `action_new_bundle_id_{user_id}` - новый Bundle ID
- `waiting_name_{user_id}` - флаг ожидания ввода названия
- `waiting_bundle_id_{user_id}` - флаг ожидания ввода Bundle ID

### Преимущества новой версии

✅ Удобнее для пользователя - не нужно перезагружать архив  
✅ Быстрее - все изменения применяются за один проход  
✅ Нагляднее - видно все запланированные действия  
✅ Гибче - можно отменить действия до применения  
✅ Эффективнее - экономия трафика и времени  

---

## Версия 1.0 - Первый релиз

### Возможности:
- Увеличение версии и билда в проекте Xcode
- Изменение названия приложения
- Изменение Bundle ID приложения
- Работа с zip архивами
- Валидация Bundle ID

